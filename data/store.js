const users={}; // this stores all the users -- "u1":{name, email}
const groups={};//stores groups and their users
const expenses=[]; //stores all expenses created
const balances={};  //from user to user how much they owe

module.exports={
    users,
    groups,
    expenses,
    balances
};